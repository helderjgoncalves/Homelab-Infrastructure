services:
  devbox:
    image: devbox:latest
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USERNAME: dev
        USER_UID: 1000
        USER_GID: 1000
    container_name: devbox
    hostname: devbox
    restart: unless-stopped
    ports:
      - "2222:22"
    dns:
      - 192.168.1.2   # Pi-hole IP
    volumes:
      - ./home:/home/dev
      - ..:/projects
      - /share:/share
    environment:
      - TZ=Europe/Lisbon
