# Uptime Kuma — environment template
# Copy to `.env` (gitignored) for shell deploys, or set the same keys in
# Portainer's stack environment-variables UI for Git-backed deploys.

# ---- Storage paths (host paths) -------------------------------------------
# Uptime Kuma state: SQLite DB, monitor history, screenshots. Small but
# write-heavy — keep on SSD.
KUMA_DIR=
